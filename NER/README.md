# 🔍 NER - Извлечение имен и фамилий

Система для извлечения имени и фамилии из текста с помощью модели **qwen2.5:14b** через Ollama.

## 🚀 Быстрый старт

### 1. Установка

```bash
# Запустите скрипт установки
./setup.sh
```

### 2. Запуск веб-интерфейса

```bash
streamlit run ner_web_interface.py
```

Откройте в браузере: **http://localhost:8501**

## 💻 Использование в Python

```python
from ner_extraction_ollama import NameExtractorOllama

# Создание экстрактора
extractor = NameExtractorOllama(model_name="qwen2.5:14b")

# Извлечение имени и фамилии
text = "Меня зовут Иван Петров, я работаю программистом"
result = extractor.extract(text)

print(f"Имя: {result['first_name']}")        # Иван
print(f"Фамилия: {result['last_name']}")     # Петров
print(f"Остаток: {result['remaining_text']}") # , я работаю программистом
```

## 📋 Возможности

### Веб-интерфейс (Streamlit)
- ✅ Обработка одиночных текстов
- ✅ Пакетная обработка
- ✅ Загрузка и обработка Excel/CSV файлов
- ✅ Экспорт результатов
- ✅ Статистика

### Python API
- ✅ Простое API
- ✅ Пакетная обработка
- ✅ Настраиваемые модели

## 📊 Примеры

### Формальные документы
```python
result = extractor.extract("Заявление от Ивана Петрова о переводе средств")
# Имя: Иван, Фамилия: Петров
```

### Email
```python
result = extractor.extract("От: Анна Сидорова <anna@mail.com>")
# Имя: Анна, Фамилия: Сидорова
```

### Пакетная обработка
```python
texts = [
    "Документ от Ивана Петрова",
    "Письмо Анны Сидоровой",
    "Заявление Михаила Козлова"
]
results = extractor.extract_batch(texts)
```

## ⚙️ Требования

- Python 3.8+
- Ollama
- 8-10 GB RAM (для qwen2.5:14b)

## 🔧 Установка вручную

```bash
# 1. Установите Ollama
curl -fsSL https://ollama.com/install.sh | sh

# 2. Загрузите модель
ollama pull qwen2.5:14b

# 3. Установите Python зависимости
pip install -r requirements.txt

# 4. Запустите Ollama
ollama serve

# 5. Запустите веб-интерфейс
streamlit run ner_web_interface.py
```

## 🎯 Выбор модели

| Модель | Качество | RAM | Скорость |
|--------|----------|-----|----------|
| qwen2.5:14b | ⭐⭐⭐⭐⭐ | 8-10GB | Средняя |
| qwen2.5:7b  | ⭐⭐⭐⭐ | 4-6GB | Быстрая |

```python
# Изменение модели
extractor = NameExtractorOllama(model_name="qwen2.5:7b")
```

## 🐛 Устранение проблем

### Ollama не запущен
```bash
ollama serve
```

### Модель не найдена
```bash
ollama list
ollama pull qwen2.5:14b
```

### Порт уже занят
```bash
streamlit run ner_web_interface.py --server.port 8502
```

## 📁 Структура проекта

```
RPA/
├── ner_extraction_ollama.py  # Основной модуль
├── ner_web_interface.py      # Веб-интерфейс
├── requirements.txt          # Зависимости
├── setup.sh                  # Скрипт установки
└── README.md                 # Документация
```

## 🔒 Приватность

- ✅ Все работает локально
- ✅ Данные не отправляются в интернет
- ✅ Не нужны API ключи

---

**Готово к использованию!** 🎉


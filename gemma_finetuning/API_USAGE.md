# 🚀 API для тестирования модели

## 📋 Что это?

Веб-интерфейс и REST API для ручного тестирования fine-tuned модели Gemma.

## 🎯 Возможности:

- ✅ **Веб-интерфейс** - красивый UI для тестирования
- ✅ **REST API** - для программного доступа
- ✅ **Автоматическая загрузка** последнего checkpoint
- ✅ **Словарь кодов** - показывает описание операции
- ✅ **Примеры** - готовые тексты для тестирования

## 🚀 Запуск:

### Вариант 1: Через скрипт
```bash
cd /home/zarina/Work/RPA/gemma_finetuning
./start_api.sh
```

### Вариант 2: Напрямую
```bash
cd /home/zarina/Work/RPA/gemma_finetuning
conda activate rpa
python api_server.py
```

### ⏱️ Загрузка модели: 1-2 минуты

Вы увидите:
```
📥 Загрузка модели...
   (это займет 1-2 минуты)
Loading checkpoint shards: 100%
   ✅ Модель загружена и готова!

🌐 Сервер запущен!
📱 Веб-интерфейс: http://localhost:8000
📡 API docs: http://localhost:8000/docs
```

## 🌐 Использование:

### 1. Веб-интерфейс (рекомендуется):

Откройте в браузере:
```
http://localhost:8000
```

**Что можно:**
- Ввести текст платежа
- Нажать кнопку "Определить код операции"
- Увидеть предсказанный код и описание
- Использовать готовые примеры

### 2. REST API:

#### Предсказание кода:
```bash
curl -X POST http://localhost:8000/predict \
  -H "Content-Type: application/json" \
  -d '{"payment_comment": "PURCHASE OF GOODS"}'
```

**Ответ:**
```json
{
  "input_text": "PURCHASE OF GOODS",
  "predicted_code": "10151",
  "code_description": "Категория продукция",
  "model_response": "10151",
  "confidence": "Высокая",
  "checkpoint": "checkpoint-3000"
}
```

#### Список всех кодов:
```bash
curl http://localhost:8000/codes
```

#### Проверка здоровья:
```bash
curl http://localhost:8000/health
```

### 3. Swagger UI (интерактивная документация):

```
http://localhost:8000/docs
```

## 📊 Текущее качество модели:

**Checkpoint: 3000** (~18% первой эпохи)
- Accuracy: ~15%
- Работает лучше для частых классов (80101, 21601)
- Качество улучшится по мере обучения

**Ожидаемое качество:**
- После 1 эпохи: ~50-65%
- После 3 эпох: ~70-85%

## 🛠️ Настройки API:

В файле `api_server.py` можно изменить:

```python
# Порт
uvicorn.run(app, host="0.0.0.0", port=8000)

# Temperature (креативность)
temperature: float = 0.1  # 0 = детерминированно, 1 = креативно

# Максимум токенов в ответе
max_tokens: int = 10
```

## 🔄 Переключение между checkpoints:

API автоматически использует **последний checkpoint**.

Если хотите использовать конкретный:

1. Откройте `api_server.py`
2. Измените строку:
```python
LATEST_CHECKPOINT = Path("outputs/gemma_qlora_20251104_181124/checkpoint-3000")
```

## ⚠️ Важно:

- **Модель занимает ~5-6 GB VRAM**
- **Первая генерация медленнее** (компиляция CUDA kernels)
- **Нельзя запускать одновременно** с обучением (конфликт GPU)
- Для production используйте больше workers

## 🐛 Устранение проблем:

**"Address already in use":**
```bash
# Найти процесс на порту 8000
lsof -ti:8000 | xargs kill -9
```

**"CUDA out of memory":**
- Остановите обучение
- Или используйте CPU (медленно):
  ```python
  device_map="cpu"
  ```

**Модель не находит checkpoint:**
```bash
ls outputs/gemma_qlora_*/checkpoint-*
```

## 📝 Примеры запросов:

**Python:**
```python
import requests

response = requests.post(
    "http://localhost:8000/predict",
    json={"payment_comment": "PURCHASE OF GOODS"}
)
print(response.json())
```

**JavaScript:**
```javascript
fetch('http://localhost:8000/predict', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({payment_comment: 'PURCHASE OF GOODS'})
})
.then(r => r.json())
.then(data => console.log(data));
```

## 🎨 Кастомизация веб-интерфейса:

В `api_server.py` найдите функцию `root()` - там весь HTML/CSS/JS.

Можете изменить:
- Цвета (градиенты)
- Примеры для тестирования
- Текст кнопок
- Стили

## 📞 Остановка сервера:

**Ctrl + C** в терминале

---

**Сделано с ❤️ для удобного тестирования модели!**


# 🔍 Руководство по мониторингу обучения

## 🎯 Как понять, что все идет хорошо?

### ✅ Признаки здорового обучения

#### 1. GPU Метрики
```
✅ GPU Utilization: 70-100%
   - Если < 30% → проблема, GPU не работает
   - Если 30-70% → можно улучшить
   - Если > 70% → отлично!

✅ GPU Memory: 80-95% занято
   - Если < 60% → batch size можно увеличить
   - Если 80-95% → оптимально для 16GB
   - Если > 95% → риск OOM

✅ Temperature: < 85°C
   - Если < 75°C → отлично
   - Если 75-85°C → нормально
   - Если > 85°C → проверьте охлаждение
```

#### 2. Training Loss
```
✅ Должен СНИЖАТЬСЯ со временем

Хорошие значения:
  Начало:  3.0-5.0  (первые 100 шагов)
  Через час: 1.5-2.5
  Через 12ч: 0.8-1.5
  Конец:   0.3-0.8  (после 1-2 дней)

⚠️ Плохие паттерны:
  - Loss растет → уменьшите learning rate
  - Loss = NaN → обучение сломалось, перезапустите
  - Loss застрял (не меняется) → проблема с данными или моделью
```

#### 3. Eval Loss
```
✅ Должен быть близко к Train Loss

Хорошо:
  Train: 0.85  Eval: 0.95  (разница < 20%)

⚠️ Плохо (Overfitting):
  Train: 0.40  Eval: 1.20  (eval >> train)
  
Решение overfitting:
  - Увеличить weight_decay
  - Добавить dropout
  - Уменьшить epochs
```

#### 4. Learning Rate
```
✅ Должен плавно изменяться

Нормальные значения:
  Начало:  ~2e-4  (warmup)
  Середина: 2e-4  (stable)
  Конец:   ~1e-5  (decay)

График должен быть:
  Warmup: ↗ (растет)
  Main:   → (стабильный)
  Decay:  ↘ (снижается)
```

#### 5. Скорость обучения
```
✅ Нормальная скорость на 16GB GPU:

  ~1-3 sec/iteration (зависит от batch size)
  ~200-500 iterations/час
  ~5000-12000 iterations/день

Если медленнее:
  - Проверьте GPU utilization
  - Уменьшите MAX_SEQ_LENGTH
  - Проверьте, не работает ли что-то на CPU
```

---

## 🛠️ Инструменты мониторинга

### Вариант 1: Автоматический мониторинг (рекомендуется)

```bash
# В отдельном терминале
conda activate rpa
cd /home/zarina/Work/RPA/gemma_finetuning
python scripts/monitor_training.py
```

**Что показывает:**
- 🎮 GPU статус (загрузка, память, температура)
- 📊 Метрики (loss, learning rate)
- 🏥 Диагностика (проблемы, предупреждения)
- 🎯 Общая оценка (все ОК / есть проблемы)

Обновляется каждые 10 секунд!

---

### Вариант 2: TensorBoard (визуальный)

```bash
# В отдельном терминале
conda activate rpa
cd /home/zarina/Work/RPA/gemma_finetuning
tensorboard --logdir=logs/
```

Откройте в браузере: **http://localhost:6006**

**Что смотреть:**
- `loss` → должен снижаться ↘
- `eval_loss` → должен снижаться и быть близко к loss
- `learning_rate` → график warmup → stable → decay
- `train/samples_per_second` → скорость обучения

---

### Вариант 3: Ручной мониторинг GPU

```bash
# Показывает GPU каждую секунду
watch -n 1 nvidia-smi
```

**Что смотреть:**
- `GPU-Util`: должно быть 70-100%
- `Memory-Usage`: должно быть ~14-15 GB из 16 GB
- `Temperature`: должно быть < 85°C

---

### Вариант 4: Логи обучения

```bash
# Смотрим последние строки лога
tail -f outputs/gemma_qlora_*/logs.txt
```

**Что искать:**
- `{'loss': ...}` → значение loss
- `{'eval_loss': ...}` → значение eval loss
- Ошибки: `Error`, `Exception`, `CUDA out of memory`

---

## 🚨 Типичные проблемы и решения

### Проблема 1: Out of Memory (OOM)
```
Симптомы:
  - CUDA out of memory error
  - Обучение крашится
  
Решение:
  1. В train_qlora.py уменьшите:
     BATCH_SIZE = 1
     GRADIENT_ACCUMULATION = 16
     MAX_SEQ_LENGTH = 256
  
  2. Перезапустите обучение
```

### Проблема 2: GPU не используется
```
Симптомы:
  - GPU-Util = 0% или очень низкий
  - Обучение очень медленное
  
Решение:
  1. Проверьте CUDA:
     python -c "import torch; print(torch.cuda.is_available())"
  
  2. Если False → переустановите PyTorch:
     pip install torch --index-url https://download.pytorch.org/whl/cu121
  
  3. Перезапустите обучение
```

### Проблема 3: Loss = NaN
```
Симптомы:
  - loss показывает NaN
  - Обучение сломалось
  
Решение:
  1. В train_qlora.py уменьшите:
     LEARNING_RATE = 1e-4  (было 2e-4)
  
  2. Или увеличьте:
     WARMUP_RATIO = 0.1  (было 0.03)
  
  3. Перезапустите с последнего checkpoint
```

### Проблема 4: Loss застрял (не меняется)
```
Симптомы:
  - Loss не меняется 1000+ steps
  - Значения одинаковые
  
Решение:
  1. Проверьте learning rate (может быть слишком маленький)
  2. Увеличьте LEARNING_RATE до 3e-4
  3. Проверьте данные (возможно проблема в датасете)
```

### Проблема 5: Overfitting
```
Симптомы:
  - Train loss << Eval loss
  - Разница больше 50%
  
Решение:
  1. Уменьшите epochs: NUM_EPOCHS = 2
  2. Увеличьте WEIGHT_DECAY = 0.01
  3. Используйте early stopping (уже встроен)
```

---

## 📊 Ожидаемый прогресс (1-2 дня обучения)

### Час 1-4 (Warmup + начало)
```
Loss: 5.0 → 2.5
GPU: 85-95%
Память: 14-15 GB
Скорость: ~2 sec/iter
```
✅ **Все ОК если:** Loss быстро падает

### Час 5-12 (Основное обучение)
```
Loss: 2.5 → 1.2
Eval Loss: появляется, близко к Train Loss
Скорость: стабильная
```
✅ **Все ОК если:** Loss стабильно снижается

### Час 13-24 (Epoch 2)
```
Loss: 1.2 → 0.7
Eval Loss: 0.8-1.0
Улучшение замедляется
```
✅ **Все ОК если:** Eval loss не растет

### Час 25-48 (Epoch 3 + завершение)
```
Loss: 0.7 → 0.4-0.6
Eval Loss: 0.6-0.8
Best model сохраняется
```
✅ **Все ОК если:** Eval loss стабилен или снижается

---

## ✅ Чеклист "Все хорошо"

Проверяйте каждые 2-4 часа:

- [ ] GPU Utilization > 70%
- [ ] GPU Memory используется 80-95%
- [ ] Temperature < 85°C
- [ ] Train Loss снижается
- [ ] Eval Loss близко к Train Loss (разница < 30%)
- [ ] Learning Rate следует графику (warmup → stable → decay)
- [ ] Скорость ~1-3 sec/iteration
- [ ] Нет ошибок в логах
- [ ] Checkpoint'ы сохраняются (каждые 500 steps)

**Если все ✅ → расслабьтесь, все идет отлично!** 🎉

**Если есть ❌ → см. раздел "Типичные проблемы"** ⬆️

---

## 🎯 Когда остановить обучение?

### ✅ Хорошие причины остановить:

1. **Обучение завершилось** (3 epochs)
2. **Early stopping сработал** (eval loss не улучшается)
3. **Достигли целевой accuracy** (проверьте через evaluate.py)

### ⚠️ Плохие причины остановить:

1. "Loss не падает уже 30 минут"
   → Подождите минимум 2-4 часа

2. "Eval loss хуже train loss"
   → Это нормально! Разница до 30% OK

3. "Слишком долго"
   → Fine-tuning BERT занимает часы, Gemma - дни

### 🛑 Когда НУЖНО остановить:

1. Loss = NaN (сломалось)
2. CUDA Out of Memory (не хватает памяти)
3. GPU перегревается (> 90°C)
4. Явная ошибка в логах

---

## 💡 Pro Tips

1. **Запустите TensorBoard сразу**
   ```bash
   tensorboard --logdir=logs/
   ```
   Оставьте открытым, смотрите графики периодически

2. **Используйте tmux/screen**
   ```bash
   tmux new -s training
   python scripts/train_qlora.py
   # Ctrl+B, потом D для отключения
   # tmux attach -t training для возврата
   ```
   Обучение продолжится даже если закроете терминал

3. **Мониторинг в отдельном терминале**
   ```bash
   python scripts/monitor_training.py
   ```
   Видите все метрики в одном месте

4. **Сохраняйте логи GPU**
   ```bash
   nvidia-smi dmon -s pucvmet > gpu_log.txt &
   ```
   Полная история использования GPU

---

## 🎬 Готовы к запуску?

1. Запустите обучение:
   ```bash
   python scripts/train_qlora.py
   ```

2. В отдельном терминале запустите мониторинг:
   ```bash
   python scripts/monitor_training.py
   ```

3. Опционально - TensorBoard:
   ```bash
   tensorboard --logdir=logs/
   ```

4. Проверяйте статус каждые 2-4 часа

5. Ждите 1-2 дня ⏰

6. Profit! 🎉

---

**Удачи с обучением! Все будет хорошо! 💪**


# 📋 Обзор проекта QLoRA Fine-tuning Gemma 3:12b

## ✅ Что создано

Полный пайплайн для fine-tuning Gemma 3:12b на задачу классификации банковских платежей.

### 📂 Структура проекта

```
gemma_finetuning/
│
├── 📁 data/                        # Директория для датасетов
│   └── .gitkeep                    # (после prepare_data.py появятся train/val/test)
│
├── 📁 models/                      # Сохраненные модели (LoRA адаптеры)
│   └── .gitkeep                    # (после обучения появится gemma_qlora_*)
│
├── 📁 outputs/                     # Результаты evaluation
│   └── .gitkeep                    # (результаты оценки)
│
├── 📁 logs/                        # TensorBoard логи
│   └── .gitkeep                    # (логи обучения)
│
├── 📁 scripts/                     # Основные скрипты
│   ├── prepare_data.py             # ⭐ Подготовка данных
│   ├── train_qlora.py              # ⭐ QLoRA fine-tuning
│   ├── evaluate.py                 # ⭐ Оценка модели
│   ├── inference.py                # ⭐ Использование модели
│   └── check_environment.py        # 🔧 Проверка окружения
│
├── requirements.txt                # 📦 Зависимости
├── README.md                       # 📖 Полная документация
├── PROJECT_OVERVIEW.md             # 📋 Этот файл
├── quickstart.sh                   # 🚀 Скрипт быстрого старта
└── .gitignore                      # Git ignore правила
```

## 🎯 Задача

**Классификация:** `PaymentComment → OperCode`

- **Входные данные:** 388,706 транзакций из `final_dataset.csv`
- **Классов:** 86 (OperCode)
- **Языки:** Русский + Английский
- **Метод:** QLoRA Fine-tuning Gemma 3:12b

## 🚀 Быстрый старт

### Вариант 1: Автоматический (рекомендуется)

```bash
# Активируйте conda окружение
conda activate rpa

# Установите зависимости
pip install -r requirements.txt

# Запустите всё одной командой
./quickstart.sh
```

### Вариант 2: Пошаговый

```bash
# Активация окружения
conda activate rpa

# Установка зависимостей
pip install torch --index-url https://download.pytorch.org/whl/cu121
pip install -r requirements.txt

# Шаг 1: Проверка
python scripts/check_environment.py

# Шаг 2: Подготовка данных
python scripts/prepare_data.py

# Шаг 3: Обучение (1-2 дня)
python scripts/train_qlora.py

# Шаг 4: Оценка
python scripts/evaluate.py

# Шаг 5: Inference
python scripts/inference.py
```

## 🔧 Конфигурация для 16GB VRAM

Оптимизированные параметры уже установлены в `train_qlora.py`:

```python
# QLoRA параметры
LORA_R = 16                    # Ранг LoRA
LORA_ALPHA = 32                # Alpha
USE_4BIT = True                # 4-bit квантизация

# Batch параметры
BATCH_SIZE = 2                 # Маленький batch
GRADIENT_ACCUMULATION = 8      # Эффективный batch = 16

# Оптимизация памяти
GRADIENT_CHECKPOINTING = True  # Экономия памяти
OPTIM = "paged_adamw_8bit"     # Эффективный оптимизатор
MAX_SEQ_LENGTH = 512           # Макс. длина
```

**Ожидаемое потребление памяти:** ~14-15 GB (влезет в 16GB!)

## 📊 Ожидаемые результаты

| Метрика | Значение |
|---------|----------|
| **Accuracy** | 90-95% |
| **F1-weighted** | 89-94% |
| **F1-macro** | 75-85% |
| **Training time** | 1-2 дня |
| **Inference** | ~1-2 сек/пример |
| **Model size** | ~200-500 MB (только LoRA) |

## 📝 Основные скрипты

### 1️⃣ prepare_data.py
**Что делает:**
- Загружает `final_dataset.csv`
- Создает stratified split (70/15/15)
- Генерирует промпты для Gemma
- Сохраняет в CSV и JSONL форматах

**Выход:**
- `data/train.jsonl` (~272k примеров)
- `data/val.jsonl` (~58k примеров)  
- `data/test.jsonl` (~58k примеров)
- `data/metadata.json`

### 2️⃣ train_qlora.py
**Что делает:**
- Загружает Gemma 2 9B (или Gemma 3:12b)
- Применяет 4-bit квантизацию
- Настраивает LoRA адаптеры
- Обучает модель
- Сохраняет адаптеры + конфигурацию

**Мониторинг:**
```bash
# В отдельном терминале
tensorboard --logdir=logs/
```

**Выход:**
- `models/gemma_qlora_TIMESTAMP/` (LoRA адаптеры ~200-500MB)

### 3️⃣ evaluate.py
**Что делает:**
- Загружает обученную модель
- Тестирует на test set
- Вычисляет метрики
- Сохраняет результаты

**Метрики:**
- Accuracy
- F1-score (weighted, macro)
- Precision, Recall
- Classification report

### 4️⃣ inference.py
**Что делает:**
- Загружает модель
- Предоставляет интерфейсы:
  - Демо режим (примеры)
  - Интерактивный режим
  - API для использования в коде

**Пример использования:**
```python
from inference import OperCodeClassifier

classifier = OperCodeClassifier("models/gemma_qlora_TIMESTAMP")
opercode = classifier.predict("TRANSFER OF FUNDS TO OWN ACCOUNT")
print(f"OperCode: {opercode}")
```

## 🎓 Технические детали

### Промпт формат
```
<start_of_turn>user
Определи код операции (OperCode) для следующего банковского платежа:

Платёж: [текст платежа]

Ответь только числовым кодом операции.<end_of_turn>
<start_of_turn>model
[OperCode]<end_of_turn>
```

### QLoRA (Quantized LoRA)
- **Базовая модель:** Квантизована в 4-bit (NF4)
- **LoRA адаптеры:** Обучаются в bfloat16
- **Параметров для обучения:** ~0.5-1% от всех параметров
- **Экономия памяти:** ~4x по сравнению с full fine-tuning

### Стратегия обучения
1. Stratified split (сохраняет баланс классов)
2. Class weights (для дисбаланса)
3. Gradient accumulation (эффективный большой batch)
4. Warmup + cosine schedule
5. Early stopping (best model сохраняется)

## 🐛 Troubleshooting

### Out of Memory
```python
# В train_qlora.py измените:
BATCH_SIZE = 1
GRADIENT_ACCUMULATION = 16
MAX_SEQ_LENGTH = 256
```

### Медленное обучение
1. Проверьте использование GPU: `nvidia-smi`
2. Установите flash-attention (опционально)
3. Уменьшите MAX_SEQ_LENGTH до 256

### Низкая точность
1. Увеличьте NUM_EPOCHS: 3 → 5
2. Увеличьте LORA_R: 16 → 32
3. Добавьте data augmentation для редких классов

## 📈 Следующие шаги

### После обучения:
1. ✅ Evaluate на test set
2. ✅ Анализ confusion matrix
3. ✅ Тестирование inference
4. 🔄 Оптимизация гиперпараметров (если нужно)

### Продакшн deployment:
1. Конвертировать в ONNX (опционально)
2. Квантизация для inference (опционально)
3. API обертка (FastAPI)
4. Мониторинг качества

## 💡 Советы по улучшению

### Если точность < 90%:
- Увеличьте epochs (3 → 5)
- Увеличьте LoRA rank (16 → 32)
- Добавьте augmentation
- Используйте class weights

### Если долго обучается:
- Уменьшите MAX_SEQ_LENGTH
- Используйте меньше данных для первых экспериментов
- Установите flash-attention-2

### Если нестабильное обучение:
- Уменьшите learning rate (2e-4 → 1e-4)
- Увеличьте warmup_ratio (0.03 → 0.1)
- Добавьте weight_decay

## 📚 Дополнительные ресурсы

- [QLoRA Paper](https://arxiv.org/abs/2305.14314)
- [Gemma Documentation](https://ai.google.dev/gemma)
- [PEFT Library](https://github.com/huggingface/peft)
- [Transformers Docs](https://huggingface.co/docs/transformers)

## ✅ Checklist перед запуском

- [ ] Conda окружение активировано (`conda activate rpa`)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] PyTorch с CUDA работает (`python -c "import torch; print(torch.cuda.is_available())"`)
- [ ] GPU доступна (16GB VRAM минимум)
- [ ] `final_dataset.csv` в корне проекта
- [ ] Свободно ~30GB на диске
- [ ] Запущена проверка окружения (`python scripts/check_environment.py`)

## 🎯 Готово к запуску!

Всё настроено и готово. Просто следуйте инструкциям в **Быстрый старт** выше.

**Удачи в обучении! 🚀**

---

**Проект:** RPA SWIFT Transaction Classification  
**Дата создания:** 2025-11-04  
**Автор:** AI Assistant  
**Для вопросов:** См. README.md


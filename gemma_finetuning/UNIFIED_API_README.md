# 🚀 Unified Banking API

Объединенный API сервер для анализа банковских операций:
- **OperCode Prediction** - определение кодов операций (Gemma 2 9B)
- **Name Extraction** - извлечение имен и фамилий (Qwen 2.5 14B)
- **Unified Analysis** - комплексный анализ обеими моделями

## 📋 Требования

### Hardware:
- **GPU:** NVIDIA с 8+ GB VRAM (для Gemma)
- **RAM:** 16+ GB (для обеих моделей)
- **CUDA:** 11.8+ или 12.x

### Software:
- Python 3.10+
- Ollama (для NER)
- CUDA toolkit

## 🔧 Установка

### 1. Установите Ollama (для NER):

```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### 2. Загрузите модель для NER:

```bash
ollama pull qwen2.5:14b
```

### 3. Запустите Ollama сервер:

```bash
ollama serve
```

Оставьте этот терминал открытым.

### 4. Проверьте Python зависимости:

```bash
# Должны быть установлены:
pip list | grep -E "torch|transformers|fastapi|uvicorn|peft|bitsandbytes|accelerate|ollama"
```

Если чего-то не хватает:

```bash
pip install torch transformers fastapi uvicorn peft bitsandbytes accelerate ollama
```

## 🚀 Запуск

### Вариант 1: Через скрипт (рекомендуется)

```bash
cd /home/user/Desktop/RPA/gemma_finetuning
./start_unified_api.sh
```

### Вариант 2: Напрямую

```bash
cd /home/user/Desktop/RPA/gemma_finetuning
python unified_api_server.py
```

### ⏱️ Время запуска: 1-2 минуты

Вы увидите:
```
================================================================================
🚀 ЗАПУСК UNIFIED API СЕРВЕРА
================================================================================
📂 Используется checkpoint: checkpoint-3000
📖 Загружено кодов в словаре: 114

📥 Загрузка Gemma модели для OperCode...
   ✅ Gemma модель загружена!

📥 Инициализация NER системы...
   ✅ NER система готова!

================================================================================
🌐 UNIFIED API СЕРВЕР ЗАПУЩЕН!
================================================================================
📱 Веб-интерфейс: http://localhost:8000
📡 API docs: http://localhost:8000/docs
```

## 🌐 Использование

### 1. Веб-интерфейс (самый простой способ)

Откройте в браузере:
```
http://localhost:8000
```

Вы увидите три вкладки:

#### 💳 OperCode Predictor
- Определение кода операции по описанию платежа
- Показывает код и его описание из словаря

#### 👤 Name Extractor
- Извлечение имени и фамилии из текста
- Возвращает имя, фамилию и остальной текст

#### 🔄 Unified Analysis
- Комплексный анализ обеими моделями
- Можно выбрать какие функции использовать

### 2. REST API

#### Предсказание кода операции:

```bash
curl -X POST http://localhost:8000/predict/opercode \
  -H "Content-Type: application/json" \
  -d '{"payment_comment": "PURCHASE OF GOODS"}'
```

**Ответ:**
```json
{
  "input_text": "PURCHASE OF GOODS",
  "predicted_code": "10151",
  "code_description": "Категория продукция",
  "confidence": "Высокая",
  "checkpoint": "checkpoint-3000"
}
```

#### Извлечение имени и фамилии:

```bash
curl -X POST http://localhost:8000/predict/ner \
  -H "Content-Type: application/json" \
  -d '{"text": "Меня зовут Иван Петров"}'
```

**Ответ:**
```json
{
  "first_name": "Иван",
  "last_name": "Петров",
  "full_name": "Иван Петров",
  "remaining_text": ""
}
```

#### Комплексный анализ:

```bash
curl -X POST http://localhost:8000/predict/unified \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Иван Петров совершил перевод средств на свой счет",
    "extract_opercode": true,
    "extract_names": true
  }'
```

**Ответ:**
```json
{
  "input_text": "Иван Петров совершил перевод средств на свой счет",
  "opercode": {
    "predicted_code": "40101",
    "code_description": "Переводы на свой счет",
    "confidence": "Высокая"
  },
  "ner": {
    "first_name": "Иван",
    "last_name": "Петров",
    "full_name": "Иван Петров",
    "remaining_text": "совершил перевод средств на свой счет"
  }
}
```

### 3. Python API

```python
import requests

# OperCode prediction
response = requests.post(
    "http://localhost:8000/predict/opercode",
    json={"payment_comment": "PURCHASE OF GOODS"}
)
print(response.json())

# Name extraction
response = requests.post(
    "http://localhost:8000/predict/ner",
    json={"text": "Меня зовут Иван Петров"}
)
print(response.json())

# Unified analysis
response = requests.post(
    "http://localhost:8000/predict/unified",
    json={
        "text": "Иван Петров совершил перевод средств",
        "extract_opercode": True,
        "extract_names": True
    }
)
print(response.json())
```

## 📊 Производительность

### OperCode Prediction (Gemma):
- Первый запрос: ~10-15 сек (компиляция CUDA)
- Последующие: **3-5 секунд** ⚡
- VRAM: ~5-6 GB

### Name Extraction (Qwen):
- Каждый запрос: **5-10 секунд**
- RAM: ~8-10 GB

### Unified Analysis:
- Время: **10-20 секунд** (обе модели последовательно)

## 🛠️ Дополнительные endpoints

### Проверка здоровья:
```bash
curl http://localhost:8000/health
```

### Список всех кодов:
```bash
curl http://localhost:8000/codes
```

### API документация (Swagger):
```
http://localhost:8000/docs
```

## ⚠️ Важно

- **Модели занимают много памяти:**
  - Gemma: ~5-6 GB VRAM
  - Qwen: ~8-10 GB RAM
  - Итого: нужно 8+ GB VRAM + 16+ GB RAM

- **Первая генерация медленнее** (компиляция CUDA kernels)

- **Нельзя запускать одновременно** с обучением (конфликт GPU)

- **Ollama должен быть запущен** для работы NER функционала

## 🐛 Устранение проблем

### "Address already in use"
```bash
lsof -ti:8000 | xargs kill -9
```

### "CUDA out of memory"
- Остановите другие процессы использующие GPU
- Или используйте CPU (медленно)

### "NER система недоступна"
```bash
# Проверьте Ollama
ollama list

# Запустите Ollama
ollama serve

# Загрузите модель
ollama pull qwen2.5:14b
```

### "Модель не находит checkpoint"
```bash
ls outputs/gemma_qlora_*/checkpoint-*
```

## 📞 Остановка сервера

**Ctrl + C** в терминале

## 🎯 Примеры использования

### Анализ банковской транзакции:
```
Текст: "Иван Петров - оплата за покупку товаров в магазине"

OperCode: 10151 (Категория продукция)
Имя: Иван
Фамилия: Петров
```

### Анализ платежного поручения:
```
Текст: "Анна Сидорова совершила перевод средств на свой счет"

OperCode: 40101 (Переводы на свой счет)
Имя: Анна
Фамилия: Сидорова
```

---

**Готово к использованию!** 🎉

Для вопросов и поддержки проверьте логи сервера.




